- var date = event.metadata.date
- var date_formatted = moment(date).utc().format('MMMM Do, YYYY')
- var content = event
header
  time(datetime=date)= date_formatted
.content
  a(href=event.metadata.link)!= content.html

